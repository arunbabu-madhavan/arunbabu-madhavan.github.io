import { DayNightColorDirective } from './day-night-color.directive';

describe('DayNightColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DayNightColorDirective();
    expect(directive).toBeTruthy();
  });
});
